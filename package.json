{
  "name": "hello-work-software-jobs",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "pnpm -r test",
    "type-check": "pnpm -r exec tsc --noEmit ",
    "lint": "pnpm exec biome lint",
    "format": "pnpm exec biome format --write --no-errors-on-unmatched"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "^2.0.6",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.5.2"
  }
}