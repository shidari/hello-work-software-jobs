services:
  collector:
    build:
      context: ../../..
      dockerfile: apps/backend/collector/Dockerfile
    ports:
      - "8080:8080"
    environment:
      JOB_STORE_ENDPOINT: http://host.docker.internal:8787
      API_KEY: dev-secret
